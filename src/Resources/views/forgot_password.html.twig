{% extends "base.html.twig" %}

{% block body %}

    {{ form_errors(form) }}

    {{ form_start(form) }}
    {{ form_rest(form) }}

    <button class="btn btn-lg btn-primary" type="submit">
        Send A One-Time Login Link
    </button>

    {{ form_end(form) }}
{% endblock %}